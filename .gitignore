# Local Claude Code settings — may contain API keys, never commit
.claude/settings.json
.claude/settings.local.json

# Secrets
.env
.env.*
!.env.example

# banana skill local state (cost ledger, presets)
.banana/
